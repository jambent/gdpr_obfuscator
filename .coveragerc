[run]
omit = *test*,__init__.py


[report]
exclude_also =
    ; Don't complain if tests don't hit defensive except code:
    except Exception as e:
    
    ;Don't complain if non-runnable code isn't run:
    if 0:
    if __name__ == .__main__.:
    

show_missing = True

skip_empty = True